# Frontend env. Copy to frontend/.env and fill in real values.
# Most of the time the defaults are fine.

# Local dev (proxied to localhost:4500 via vite.config.ts):
# VITE_API_BASE=/api/v1
#
# Production:
VITE_API_BASE=https://chilimba-production.up.railway.app/api/v1
VITE_APP_NAME=Chilimba
